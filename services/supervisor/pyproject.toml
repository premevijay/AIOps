[project]
name = "aiops-supervisor"
version = "0.1.0"
description = "AIOps NetOps Supervisor — LangGraph agent over the execution worker"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
