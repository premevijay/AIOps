[project]
name = "aiops-worker"
version = "0.1.0"
description = "AIOps connectivity worker — Phase 1 spine (CyberArk + Catalyst over NATS)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
