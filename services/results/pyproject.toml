[project]
name = "aiops-results"
version = "0.1.0"
description = "AIOps Results Store — persists worker job results for the dashboard read API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
